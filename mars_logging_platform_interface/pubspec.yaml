name: mars_logging_platform_interface
description: 'A common platform interface for the mars_logging plugin.'
version: 1.0.0-dev.0
repository: 'https://github.com/yanshouwang/mars_logging'
issue_tracker: 'https://github.com/yanshouwang/mars_logging/issues'
topics:
  - mars
  - logging

environment:
  sdk: ^3.7.2
  flutter: '>=3.3.0'
resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
